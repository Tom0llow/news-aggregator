interface:
  display_name: "PR Review"
  short_description: "Independent final PR review with bounded fix/re-review"
  default_prompt: "Follow the pr-review workflow for the user's request."

policy:
  allow_implicit_invocation: false
